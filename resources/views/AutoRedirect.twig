<script>
    (function() {
        console.log('VR Payment: Redirecting to payment page...');
        var redirectUrl = {{ redirectUrl|json_encode|raw }};
        console.log('VR Payment URL:', redirectUrl);
        if (redirectUrl) {
            window.location.href = redirectUrl;
        }
    })();
</script>
